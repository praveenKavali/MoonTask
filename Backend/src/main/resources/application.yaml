spring:
  application:
    name: Backend
  datasource:
    url: jdbc:postgresql://localhost:5432/spring
    username: "praveen"
    password: "000000"
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: praveenkumar123291@gmail.com
    password: gvlnhtsmxmvmarbc
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

logging:
  level:
    org:
      hibernate:
        type:
          descriptor:
            sql:
              BasicBinder: TRACE
      springframework:
        security: DEBUG



